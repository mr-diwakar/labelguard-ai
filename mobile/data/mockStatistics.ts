import { InspectionSummary } from '../types/inspection';

/** Sample counters for the Home statistics row; not real user data. */
export const mockInspectionSummary: InspectionSummary = {
  total: 12,
  compliant: 8,
  manualReview: 3,
  potentialIssues: 1,
};
