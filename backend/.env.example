# Copy this file to .env and adjust the values for your machine.
# .env holds real settings and should never be committed.

# Network interface the API binds to. Use 0.0.0.0 to accept external traffic.
APP_HOST=127.0.0.1

# Port the API listens on.
APP_PORT=8000
